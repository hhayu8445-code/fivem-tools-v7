# API Configuration
VITE_API_KEY=your_api_key_here
VITE_APP_ID=your_app_id_here

# Discord OAuth2 Configuration
VITE_DISCORD_CLIENT_ID=your_discord_client_id
VITE_DISCORD_REDIRECT_URI=https://fivemtools.net/auth/callback

# Backend API URL (for OAuth)
VITE_BACKEND_URL=http://localhost:3000

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual credentials
# 3. Get Discord OAuth2 credentials from https://discord.com/developers/applications
# 4. Add redirect URI in Discord app settings: https://fivemtools.net/auth/callback
# 5. For production deployment on Vercel, add all env vars in Vercel dashboard
# 6. Never commit .env to version control
